#ifndef PARTICLE_WEIGHT_NORMALISATION_H
#define PARTICLE_WEIGHT_NORMALISATION_H

// Things that need to be included in the file particle_weight_normalisation.cpp
#include <Rcpp.h>
#include <vector>

// Functions Contained Within File particle_weight_normalisation.cpp
std::vector <double> Particle_Weight_Normalisation(std::vector <double> particle_weights);

#endif
